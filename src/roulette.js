import { PacMan } from './pac-man';

export function roulette (){

  let placeGo = new PacMan()

  const randomPos = [-2,2,-3,3];
  let index = Math.floor((Math.random(placeGo.playableMoves != placeGo.currentPos));

  return randomPos[index];
}
